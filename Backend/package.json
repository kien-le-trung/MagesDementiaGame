{
  "name": "mages-dementia-game-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that evaluates Lan's natural-language approach through OpenRouter.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.68.0"
  }
}
